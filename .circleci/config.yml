version: 2.1
orbs:
  node: circleci/node@1.1
jobs:
  build:
    executor:
      name: node/default
      tag: '10.4'
    ssds:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
      - run: npm run test
